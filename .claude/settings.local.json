{
  "permissions": {
    "allow": [
      "Skill(claude-api)"
    ]
  },
  "commands": {
    "stp": {
      "command": "cd /opt/stockpush && python3 -m stockpush.worker --hermes $ARGUMENTS --output text 2>/dev/null",
      "description": "StockPush control: start, stop, status, signals, test"
    }
  }
}
